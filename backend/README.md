# Pet Adoption Backend

Node/Express backend for Pet Adoption Management System.

Setup

1. Copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`.
2. Install dependencies:

```bash
npm install
```

3. Run in dev:

```bash
npm run dev
```

API endpoints live under `/api` (auth, pets, applications).
